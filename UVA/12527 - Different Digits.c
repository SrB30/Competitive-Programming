#include <stdio.h>

int main()
{
    int n, m, i, diff[5001];
    diff[1] = 1;
    for(i=2; i<=5000; i++)
    {
        itoa(i);
        for(j=strlen(i)-1; j>=0; j--)
        {
            for(k=0; k<=(j-1); k++)
            {
                if(j =)
            }
        }
    }
    while (scanf("%d%d", &n, &m) != EOF)
    {

    }
}
